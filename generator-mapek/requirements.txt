# Minimal Python requirements for generate_map.py
# Dopięte na sztywno (nie >=) — osmnx regularnie zmienia zachowanie między
# wersjami (m.in. features_from_polygon), a niedopięte wersje dawały ciche
# rozjazdy zachowania między maszynami przy oddzielnych `pip install`
# w różnym czasie (ten sam kod, bez błędu, różny wynik — patrz STAN-PRAC.md).
# Wersje = to, co faktycznie przetestowane i działające.
requests==2.32.5
geopandas==1.0.1
shapely==2.0.7
matplotlib==3.9.4
osmnx==2.0.7

# Optional (requires compilation or conda packages):
# pyrosm>=0.6.2  # optional: PBF extraction (may fail to build on some systems)
